import { loading } from '../../components';
import md from '../../components/loading/README.md';
import { CONFIG } from '../configType';

const config: CONFIG = {
  name: 'Loading',
  render: loading,
  prop: {
    
  },
  explain: md
};
export default config;
